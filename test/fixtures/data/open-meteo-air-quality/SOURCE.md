# Open-Meteo Air Quality fixture provenance

This synthetic JSON fixture reproduces the documented Open-Meteo Air Quality API response
shape for one coordinate, including grid-cell metadata, GMT hourly timestamps, units, numeric
values, and a documented nullable model value. Coordinates and values are invented for
deterministic tests; no live response or user data is stored here.
