# Open-Meteo Flood fixture provenance

This synthetic JSON fixture reproduces the documented Open-Meteo Flood API response shape for
one coordinate, including a selected river-grid coordinate, GMT daily dates, units, nullable
simulated discharge values, and representative ensemble-member fields. Coordinates and values
are invented for deterministic tests; no live response or user data is stored here.
